# CMake folders
build
build_*

# OS generated files
.DS_Store
*.swp

# Bazel generated files
bazel-*

# Experimental O3DE/Atom backend (see o3de/README.md): the vendored upstream
# O3DE engine tree and scratch build dirs are external and must not be tracked.
vendor/
test_build*/
imgui.ini

